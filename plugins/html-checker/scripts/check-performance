#!/usr/bin/env bun

/**
 * check-performance - Analyze performance-related HTML patterns
 * Executable wrapper for the html-checker plugin
 */

import { join } from "node:path";

const scriptDir = import.meta.dir;
const mainScript = join(scriptDir, "src", "check-performance.ts");

// Re-export by importing and running
await import(mainScript);
