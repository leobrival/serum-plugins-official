#!/usr/bin/env bun

/**
 * check-headings - Analyze heading hierarchy (H1-H6)
 * Executable wrapper for the html-checker plugin
 */

import { join } from "node:path";

const scriptDir = import.meta.dir;
const mainScript = join(scriptDir, "src", "check-headings.ts");

// Re-export by importing and running
await import(mainScript);
